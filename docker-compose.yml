services:
  vitest-ui:
    image: mcr.microsoft.com/playwright:v1.57.0-jammy
    container_name: vitest-ui
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "npm install && npm run test:ui:ci"
    ports:
      - "51204:51204"
